name: hello world

on:
  # push:
  #   branches: [main]
  workflow_dispatch: 

  # repository_dispatch:
  #    types:
  #      - run_tests


jobs:
  greet:
   runs-on: ubuntu-latest
   steps:
      - name: Say Hello
        run:  echo "Hello World"
